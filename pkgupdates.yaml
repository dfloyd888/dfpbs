---

#Core packages that must be at the latest code, barring some conflict or
#other restriction.

- name: Update openssl
  package: name=openssl state=latest

- name: update sshd
  package: name=openssl-libs state=latest

- name: update kernel
  package: name=kernel state=latest
